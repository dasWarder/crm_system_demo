spring:
  datasource:
    username: user
    password: pass
    url: jdbc:postgresql://localhost:9999/crm
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true