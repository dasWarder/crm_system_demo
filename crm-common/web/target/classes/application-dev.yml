spring:
  datasource:
    username: user
    password: pass
    url: jdbc:postgresql://localhost:9999/crm
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  mail:
    host: smtp.gmail.com
    username: ${MAIL_NAME}
    password: ${MAIL_PASS}
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: 3822d0d2ee84de68710e
            client-secret: da19a59b6559b23980030de4abfbfd5c92db695b

email:
  author: ${MAIL_NAME}

url:
  base: http://localhost:8080